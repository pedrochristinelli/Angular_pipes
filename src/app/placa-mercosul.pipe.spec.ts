import { PlacaMercosulPipe } from './placa-mercosul.pipe';

describe('PlacaMercosulPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacaMercosulPipe();
    expect(pipe).toBeTruthy();
  });
});
