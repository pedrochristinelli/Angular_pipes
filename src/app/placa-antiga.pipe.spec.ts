import { PlacaAntigaPipe } from './placa-antiga.pipe';

describe('PlacaAntigaPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacaAntigaPipe();
    expect(pipe).toBeTruthy();
  });
});
